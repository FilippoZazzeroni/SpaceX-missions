import 'package:flutter/material.dart';

class CustomShadow {
  static final card = BoxShadow(
      color: Colors.black26,
      blurRadius: 4,
      spreadRadius: 0.7,
      offset: Offset.fromDirection(1.57, 3.0));
}
